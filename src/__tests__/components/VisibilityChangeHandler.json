{
    "type": "Text",
    "text": "${ordinal}",
    "handleVisibilityChange": [
        {
            "description": "should fail",
            "when": "not boolean",
            "commands": []
        }
    ]
}
